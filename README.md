# checklab-front-react

This is a JavaScript (plain React/Next.js) variant scaffold copied from `checklab-front`.

Next steps:

- Convert `.ts`/`.tsx` files to `.js`/`.jsx` (automated codemods recommended)
- Remove remaining TypeScript-specific configs (`tsconfig.json`, `next-env.d.ts`) if present
- Install dependencies and run `npm run dev`

To ask me to proceed with automated conversion, reply `convert now`.
# Check-Lab-Live-React
